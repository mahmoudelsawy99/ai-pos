{
  "name": "pos-server",
  "version": "1.0.0",
  "description": "JSON Server for POS System",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --middlewares ./middleware.js --port 3000"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "json-server": "^0.17.4",
    "jsonwebtoken": "^9.0.0"
  }
}
